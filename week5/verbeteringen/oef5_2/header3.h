#define MAX(a,b) ((a > b) ? (a):(b)) //M hernoemt naar MIN en MAX om verschil aan te geven
#ifndef LASTNAME //we willen niet dat dit de originele achternaam overschrijft
#define LASTNAME "wazowski"
#endif